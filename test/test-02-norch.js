/*global describe:true, it:true, before:true, after:true */

var
    demand = require('must'),
    path   = require('path'),
    Scorched = require('../index');

describe('norch', function()
{
    it('has tests');
});
